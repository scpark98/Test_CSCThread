@../../Common/claude.md

# Test_CSCThread

Common 라이브러리의 `CSCThread` (스레드 래퍼) 동작 검증용 MFC 다이얼로그 테스트 프로젝트. `knoconnectiontester` 의 LMM 관련 헤더(`LMMAgent.h/.cpp`, `LMMEventListener.h`, `LMMDefines.h`, `KNODefines.h`) 를 복사해 들여와 사용 시나리오를 재현.

## 프로젝트 한정 메모

- (필요 시 추가)
